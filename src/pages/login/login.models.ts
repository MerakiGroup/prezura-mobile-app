export interface AlertData {
  title: string;
  subTitle: string;
}

export interface UserAuthResponse {
  id: string;
  firstName: string,
  lastName: string,
  email: string;
  imageUrl: string;
  gender: string;
}
